/**
  * @author BlueWhaleYT
  * ---
  * This is a sample Javascript code.
  * Click "Convert" to convert the code into one line.
  * ---
  */

function foo(a, b) {
    console.log(`Hello ${a}, ${b}`);
}

let a = 6, b = Math.pow(a, 2);

foo(a, b);